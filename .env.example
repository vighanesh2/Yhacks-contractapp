# Example env var names only — copy to ".env.local" and add real values.
# Never put secrets in this file; ".env.example" is safe to commit.

NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

ANTHROPIC_API_KEY=
# ANTHROPIC_ANALYZE_MODEL=

OPENAI_API_KEY=
